# Pinned deliberately. The .pkl artifacts in production/ were created with
# scikit-learn 1.8.0; loading them under a different version raises
# InconsistentVersionWarning and risks silently wrong predictions, because
# unpickled estimators are not guaranteed to round-trip across versions.
# Re-pin only when the model is retrained and re-exported.
scikit-learn==1.8.0
pandas==3.0.3
numpy==2.4.4
imbalanced-learn==0.14.2

# Model inference API
fastapi==0.141.1
uvicorn==0.51.0
pydantic==2.13.0
